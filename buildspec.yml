version: 0.2

phases:
  pre_build:
    commands: 
      - aws ecr get-login-password --region us-east-2 | docker login --username AWS --password-stdin 607349672698.dkr.ecr.us-east-2.amazonaws.com
  build:
    commands: 
      - docker build -t confirmation .
      - docker tag confirmation:latest 607349672698.dkr.ecr.us-east-2.amazonaws.com/confirmation:latest
  post_build:
    commands:
      - docker push 607349672698.dkr.ecr.us-east-2.amazonaws.com/confirmation:latest